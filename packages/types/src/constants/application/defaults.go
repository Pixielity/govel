package constants

// Application Default Settings
// These constants define the default values used throughout the GoVel application
// when no explicit configuration is provided.

const (
	// DefaultEnvironment is the default application environment
	DefaultEnvironment = "development"

	// DefaultName is the default application name
	DefaultName = "GoVel Application"

	// DefaultVersion is the default application version
	DefaultVersion = "1.0.0"

	// DefaultLocale is the default application locale for internationalization
	DefaultLocale = "en"

	// DefaultFallbackLocale is the fallback locale when the primary locale is unavailable
	DefaultFallbackLocale = "en"

	// DefaultTimezone is the default application timezone
	DefaultTimezone = "UTC"

	// DefaultDebug indicates the default debug mode setting
	DefaultDebug = true

	// DefaultRunningInConsole indicates the default console mode setting
	DefaultRunningInConsole = false

	// DefaultRunningUnitTests indicates the default unit testing mode setting
	DefaultRunningUnitTests = false
)