package interfaces

import (
	shared "govel/packages/types/src/shared"
)

/**
 * MaintenanceMode is a type alias for the shared version to maintain backward compatibility.
 * The actual type definition is now in the shared package to avoid circular imports.
 */
type MaintenanceMode = shared.MaintenanceMode
